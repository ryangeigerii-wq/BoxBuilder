import pytest

@pytest.mark.skip(reason="Legacy route /box-builder-lemonade removed after consolidation; keeping file to avoid stale test discovery in CI caches.")
def test_box_builder_lemonade_route_removed():
    pass
